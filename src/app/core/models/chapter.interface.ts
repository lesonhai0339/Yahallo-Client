export interface LastestChapter{
    id: string,
    title: string,
    index: number,
    createDate: string
}
export interface ChapterImage{
    id: string,
    index: number,
    cloudUrl: string
}